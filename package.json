{
  "name": "breakouts-terminal-frontend",
  "version": "1.0.0",
  "description": "Breakouts by Claude & Deadshot — Frontend",
  "scripts": {
    "dev": "npx serve public -l 5173",
    "build": "echo 'Static site — no build needed'"
  }
}
